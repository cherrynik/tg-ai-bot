# Настройка Userbot

Userbot работает как обычный пользователь Telegram, поэтому видит **ВСЕ сообщения** в группах без ограничений Privacy Mode.

## Шаги настройки:

### 1. Получите API ID и API Hash

1. Перейдите на https://my.telegram.org/apps
2. Войдите с вашим номером телефона
3. Создайте приложение (Application)
4. Скопируйте **api_id** и **api_hash**

### 2. Настройте .env файл

Создайте файл `.env` со следующим содержимым:

```
API_ID=ваш_api_id
API_HASH=ваш_api_hash
SESSION_STRING=
OPENAI_API_KEY=ваш_openai_api_key
BOT_NAME=AI Assistant
STARTUP_MESSAGE=Привет! Я AI Assistant, готов к работе! 🚀
```

**Важно:** `SESSION_STRING` оставьте пустым при первом запуске. После авторизации бот покажет строку сессии, которую нужно будет добавить в `.env`.

### 3. Установите зависимости

```bash
yarn install
# или
npm install
```

### 4. Запустите бота

```bash
yarn start
# или
npm start
```

При первом запуске:
1. Введите номер телефона (с кодом страны, например: +79991234567)
2. Введите код подтверждения из Telegram
3. Если у вас включена двухфакторная аутентификация, введите пароль
4. Скопируйте `SESSION_STRING` из консоли и добавьте в `.env`

### 5. Перезапустите бота

После добавления `SESSION_STRING` в `.env`, перезапустите бота. Теперь авторизация будет автоматической.

## Преимущества Userbot:

✅ Видит **ВСЕ сообщения** в группах (не только упоминания)
✅ Не требует отключения Privacy Mode
✅ Работает как обычный пользователь
✅ Может быть в неограниченном количестве групп

## Безопасность:

⚠️ **ВАЖНО:** Храните `SESSION_STRING` в секрете! С этой строкой можно получить доступ к вашему аккаунту Telegram.

⚠️ Не коммитьте `.env` файл в git!

